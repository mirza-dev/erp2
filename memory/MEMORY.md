# Memory Index

## User
- [user_review_workflow.md](user_review_workflow.md) — "Bulgular" raporuyla review yapar; önce doğrula, sonra düzelt; REVIEW.md = formal review kuralları

## Project
- [project_stack.md](project_stack.md) — Stack, inline style kuralları, klasör yapısı, veri modelleri, seed/demo hazırlık; Claude Code skills: `~/.claude/skills/erp2-*` (6 skill aktif)
- [project_domain.md](project_domain.md) — Sipariş çift ekseni, stok modeli, KDV, import kontratı, alert tipleri, tamamlanan fazlar (0–13)
- [project_pmt_multi_type.md](project_pmt_multi_type.md) — PMT multi-product-type firma; tek-tip assumption YAPMA; multi-type karışık katalog/sipariş/teklif zorunlu
- [project_security.md](project_security.md) — RLS, middleware, demo mode, TÜM audit bulguları kapatıldı (C1-C3,H1-H4,M1-M4,L1,L3,B04 ✅)
- [project_rbac.md](project_rbac.md) — Rol bazlı erişim (6 rol, app_metadata.roles): Faz 1+2+4+5 MAIN'DE (merge `234d8d9`); R3 finansal redaction + R1/R2 guard'lar; quotes RBAC main'in Faz 8a'sı (delete_quotes/410-convert); Faz 6/7 ertelendi
- [project_integrations.md](project_integrations.md) — Paraşüt Faz 1-10 ✅ (1914 test), Faz 11 sırada; AI kolon eşleştirme, Sentry ✅, k6, smoke 24
- [project_auth.md](project_auth.md) — Login, kullanıcı yönetimi, admin API, landing page
- [project_products_page.md](project_products_page.md) — Veri akışı, drawer edit modu, dinamik kategori, scan lock, mount scan davranışı, perf pattern
- [current_focus.md](current_focus.md) — Son: **Ürün Tipleri — final ürün [alan-edit UI headline + N+1 fix + confirm→a11y modal]** (`/dashboard/settings/product-types` liste+`[id]` detay+4 RBAC `manage_product_types` route+`product-types.ts` helper; sayfa olgun [helper 516+route~33+helper~25+seed 12 test], güvenlik bug'ı YOK; danışman triyajı + **kullanıcı kapsam onayı "Evet ekle"**; **#1 [HEADLINE fonksiyonel boşluk] alan düzenleme UI'ı YOK'tu** [detay tablosu etiket/tip/birim/seçenek düz metin, per-row yalnız required-toggle+↑↓+Sil; `PATCH .../fields/[fieldId]`+`dbUpdateProductTypeField` düzenlemeyi TAM destekliyordu ama UI yalnız `{required}`/`{sort_order}` gönderiyordu→yazım hatası=sil+yeniden ekle=o tipteki TÜM ürünlerde attribute kaybı]→satır-içi **"Düzenle"**→edit modal `role=dialog`+`aria-modal`+`aria-labelledby="edit-field-title"`+`editDraft`/`editFieldId` state; **`field_key` READ-ONLY** [footgun: ürünlerin `attributes` JSONB kimliği→değiştirmek orphan]+tip-değiştirme caveat `var(--warning-text)`+PATCH `{label_tr,label_en,field_type,unit,options}`; paylaşılan saf **`buildFieldPayload(draft)`** [Ekle+Düzenle DRY: select→options array/non-select→null/number→unit/**non-number→null=stale unit önler**]; **#2 [MED] liste N+1 fetch+sessiz yanlış veri** [`loadTypes` her tip için `?withFields=1`=8 tip 9 istek; kısmi fail→16-alanlı Vana "0 alan"]→`dbListProductTypes` `.select("*, product_type_fields(count)")` nested-count unwrap [PostgREST `[{count:N}]`→`fieldCount:N` **ADDITIVE**—ProductTypeRow reshape edilmez/product-create selector+import extract/classify etkilenmez]+cache 60s+tag+4 field route `revalidateTag` doğrulandı+`page.tsx` N fetch kaldırıldı [9→1]; **#3 [MED] yıkıcı `confirm()`→a11y modal** [`deleteType`+`deleteField` `window.confirm()`→`confirmDeleteType`/`confirmDeleteFieldId` state+inline `role=dialog`; deleteField "ürünlerde alan kaybolur" uyarısı; `window.location.href`→`router.push`]; **ÇÜRÜTÜLEN** [danışman flag→şema okundu]: reorder `(i+1)*10` sort_order çakışmaz [056'da `UNIQUE(product_type_id,sort_order)` YOK, yalnız non-unique index+`UNIQUE(product_type_id,field_key)`]; **DROP**: field_key edit [footgun]/ürün attribute migration [backend sözleşmesi]/focus-trap/create modal maxLength [server validation]; **+20 test** [`product-types-ui.test.ts` buildFieldPayload 7 saf+detay 8 source-regex+liste 2 / `product-types-list-count.test.ts` unwrap 3], mevcut 70 backend dokunulmadı [70→92], tsc/lint/build temiz **4460→4480**; **GÜVENLİ probe 18/18** [prod `ryvxpolvhvsycuqyphoa`, **Yeni Tip/Alan Ekle/Sil/reorder + Kaydet ASLA TIKLANMADI**=MUTASYON YOK çünkü create/delete/PATCH kalıcı+kısmen geri-alınamaz; `GET /api/product-types`→9 tip HER tipte fieldCount 7/9>0 + **`Vana fieldCount=16==detay alan sayısı 16` cross-check=DECISIVE drift-proof no-op değil** + detay 16 'Düzenle' butonu render; **headline İNTERAKTİF (openEdit saf client state=güvenli): Düzenle→modal açıldı/field_key DISABLED+prefill "dn"/label_tr round-trip "DN (Nominal Çap)"/tip→select textarea görünür→tip→text gizlendi [dinamik iki yönlü]/İptal→kapandı [Kaydet TIKLANMADI=PATCH yok]**; doğrudan helper prod çağrısı Vana16/Conta13/Flans9/Fitting7/Bağlantı8/Enstrüman8/Sızdırmazlık7=seed birebir `hasNested=false`; **ilk probe `unstable_cache` bayat-entry yakaladı [önceki dev session 8-tip eski shape]→`.next/cache` temizlenip fix prod'da doğrulandı**; yalnız save-PATCH+alan-silme offline vitest+33 route testi]; **COMMIT EDİLDİ + PUSH kullanıcı onayı bekliyor** [push→Coolify prod deploy]); önceki: **Satın Alma Siparişi (Yeni) — ürün seç→birim fiyat+KDV gelmiyor [FONKSİYONEL]** (`/dashboard/purchase/orders/new`; kullanıcı ekran görüntüsü gönderdi ama macOS TCC nedeniyle OKUNAMADI→AskUserQuestion ile sayfa kesinleşti; kök neden danışman onayladı: `new/page.tsx:274` ürün `<select>` `onChange` yalnız `product_id` set ediyor, `unit_price` "0" kalıyor→`grandTotal` `qty*price` price=0→toplam 0,00; KDV ayrı bug DEĞİL=türev; #2 ekran görüntüsü GÖRÜLMEDİ, muhtemelen `:169` submit validasyonu "birim fiyat sıfırdan büyük olmalı"=aynı kök [VARSAYIM]; **#1 [HEADLINE]** pure `pickPurchaseUnitPrice(product)`=ürün `cost_price`'ı [satın alma=MALİYET; `price`=SATIŞ fallback KULLANILMADI=danışman footgun PO şişer; null→boş elle gir]+`handleProductSelect(idx,id)` product_id+unit_price birlikte+`onChange` yönlendirildi; **#2 [currency güvenlik]** koşulsuz doldur+`product.currency!==currency` satırda inline `var(--warning-text)` uyarı [OrderForm paterni; kur çevrimi YOK]; **#3 [görünürlük "kdv tutarı"]** pure `computePoTotals(lines)` {subtotal/discount/vat/grand KDV%20}+alt blok Ara Toplam·İskonto·KDV(%20)·**Tahmini Genel Toplam**+"kesin tutar kaydedilince" notu [danışman: kaydedilmemiş form=tahmin, RPC otoritatif]; veri doğrulama [danışman kritik "no-op olur mu?"]: `/api/products?all=1` `redactProductsForPerms` cost_price←`view_purchase_costs` admin/purchaser yetkili gelir+seed 7/8 ürün cost_price set; DROP: price fallback/kur çevrimi/server-route-`validatePoLines` [Faz 3 audit]/OrderForm-QuoteForm [**QuoteForm:442 currency-guard ayrı şikâyet olabilir, ayrı tur**]; +11 test `purchase-orders-ui.test.ts` genişletme [pickPurchaseUnitPrice 3+computePoTotals 5+source-regex 3], 28→41, 4449→4460; GÜVENLİ probe 10/10 [prod `ryvxpolvhvsycuqyphoa`, **"Sipariş Oluştur" ASLA TIKLANMADI** çünkü POST kalıcı purchase_orders+po_counters+audit_log; **`GET /api/products?all=1` cost_price non-null 7/8 CANLI kanıt=redaksiyon E2E engellemiyor, fix prod'da çalışır**]; **COMMIT+PUSH `2509dcf`** [`b37764a..2509dcf`] · kullanıcı netleştirmesi: birim fiyat zorunlu değil, tedarikçiye göre değişken→düzenlenebilir öneri zaten karşılıyor, AskUserQuestion→**"şimdiki hali" seçildi, kod değişmedi** · ⚠️ #2 ekran görüntüsü görülmedi); önceki: **Ayarlar sayfası final ürün** (`/dashboard/settings`; sayfa olgun [1239 satır+6 test], güvenlik bug'ı YOK; danışman dürüstlük onayı → 0 fonksiyonel headline + 3 a11y/disiplin + 1 render hatası: #1 [MED-headline] **`ResetDemoSection` onay modalı a11y eksik** [en yıkıcı aksiyon — `POST /api/seed` TÜM prod veriyi siler] `ResetDemoSection.tsx:114` iki çıplak `<div>`→panel `role=dialog`+`aria-modal`+`aria-labelledby="reset-demo-confirm-title"`+başlık id, focus-trap DROP [production/PO/customers/vendors precedent]; #2 [MED] sol sekme menüsü tablist a11y+dirty-dot text-eşdeğeri yok [`page.tsx:1190` düz button dizisi, dirty nokta yalnız görsel `<span>`]→konteyner `role=tablist`+`aria-label`+her buton `role=tab`+`aria-selected={activeTab===key}`+`id={settings-tab-${key}}`+`aria-controls`, sağ içerik `role=tabpanel`+`id=settings-tabpanel`+`aria-labelledby={settings-tab-${activeTab}}` [danışman: kırık-ARIA tuzağı için tek aktif id], dirty nokta `aria-hidden`+dirty durum erişilebilir adda [`${label} (kaydedilmemiş değişiklikler)`], roving-tabindex DROP [Tab-odaklı=WCAG bar korunur]; #3 [LOW-MED→görünür] `&apos;` JS string literal ham görünüyor [`:927` ApiTab ternary `"…&apos;Bağlan&apos;…"` JS string, JSX text DEĞİL→decode olmaz]→düz tek tırnak `'` U+0027 [danışman tuzak: `&#39;` de decode olmaz, bug tekrarlar]; grep doğrulandı yalnız :927, `:953` `Paraşüt&apos;e bağlan` JSX text DOKUNULMADI; #4 [LOW-MED] FirmaTab kayıt hatası jenerik [somut repro: client validate max-length yok, sunucu `validateCompanyPatch` >200 reddeder→spesifik 400 mesajı düşüyordu]→`errBody?.error` parse+catch `err.message` [kardeş KullaniciTab/BildirimlerTab paterni]; DROP: focus-trap/Escape, roving-tabindex, ApiTab-AiTab refactor [çalışıyor], 7 route+3 helper [denetlendi+6 test], `patchUserMetadata` GET-merge-SET race [dokümante+UI `isMutating` lock]; +10 test yeni `settings-ui.test.ts`, 6 mevcut settings test DOKUNULMADI, 4439→4449; GÜVENLİ salt-okuma probe 10/10 [prod `ryvxpolvhvsycuqyphoa`, **ResetDemoSection TIKLANMADI/onay modalı AÇILMADI** çünkü /api/seed tüm prod siler/geri-alınamaz, Firma PATCH/şifre/avatar/preferences de tetiklenmedi=MUTASYON YOK; `role=tablist` CANLI a11y kanıtı + ham `&apos;` YOK kanıtı], **COMMIT+PUSH `b37764a`**); önceki: **Cariler (Müşteriler) sayfası final ürün** (`/dashboard/customers`; sayfa olgun, güvenlik bug'ı YOK; danışman triyajı → 1 fonksiyonel + 3 disiplin: #1 [MED-headline] **toplu silme bayat satır bırakıyordu [FONKSİYONEL — danışman yakaladı, self-check kaçtı]** — `handleBulkDelete` ham fetch+`clearAll()` yalnız seçimi temizliyor, `customers` state'inden silinen satırları KALDIRMIYOR+refetch yok→"N silindi" toast'ı sonrası satırlar tabloda kalır; tek-silme `deleteCustomer`→`setCustomers filter` doğruydu=asimetri; fix bulk context `deleteCustomer` üzerinden [`allSettled`, başarı satırı filter, fail=rejected], açık panel kapatılır; **paired:** DELETE route `revalidateTag("customers")` YOK'tu [POST'ta var]→30s cache silineni geri verir→eklendi; #2 [MED] liste hover DOM-mutation antipattern [`querySelectorAll td.style.background`]→`hoveredId` state [orders/quotes/products/PO precedent]; #3 [MED] 3 çıplak modal a11y [toplu-silme+Yeni Müşteri+`CustomerDetailPanel` slide-in]→`role=dialog`+`aria-modal`+`aria-labelledby`+başlık id, focus-trap DROP; #4 [LOW-MED] validation parity — PATCH `validateStringLengths` YOK'tu [POST'ta var]+`addCustomer` ham `{"error"}` text() yerine `errBody.error` parse [kardeş paterni]; DROP: header "{N} aktif" alt-yazısı DOĞRU [liste is_active=true+hard-delete, "mislabeled" varsayımım yanlıştı], Pasif tab vestigial, Vergi precedence kozmetiği; +13 test yeni `customers-ui.test.ts`[9]+`customer-patch-route`[+4] [update-mapping/nav dokunulmadı], 4426→4439; GÜVENLİ salt-okuma probe 7/7 [prod 4 müşteri, satır→`CustomerDetailPanel role=dialog` CANLI a11y kanıtı, MUTASYON YOK — create kalıcı customers/delete kalıcı audit_log], **COMMIT+PUSH `c8057e5`**); önceki: **Üretim Girişi sayfası final ürün** (`/dashboard/production`; sayfa zaten çok olgun [Sesli Giriş V1-V3+Faz 10], güvenlik bug'ı YOK; danışman triyajı → 3 düzeltme: #1 [MED-headline] BOM eksik-bileşen detayı UI'a HİÇ ulaşmıyordu [fonksiyonel] — `complete_production` 409'u `shortages[{component_name,required,available}]` taşır ama `addUretimKaydi` yalnız jenerik error throw edip detayı düşürüyordu→kullanıcı hangi hammaddeyi bilemiyor; +danışman: zengin mesaj yalnız hepsi-başarısız dalında, **kısmi** dal sessizce yutar; fix: yeni client-safe pure `production-shortage-helpers.ts` `buildShortageMessage`+`addUretimKaydi` errBody.shortages+`handleSave` kısmi dal firstError; #2 [MED] silme=tek-tıkla sessiz stok geri-alımı [`reverse_production` envanter ters hareketi], onay YOK→`confirmDeleteId`+inline modal `role=dialog`+`aria-modal`+`aria-labelledby`+başlık id, × yalnız açar [PO `470578c` precedent, bu sayfada modal yoktu]; #3 [LOW] a11y — tarih input+satır-kaldır×+silme× aria-label; DROP: gelecek-tarih guard [domain §8 tavan yok, spekülatif — domain-rules okundu]+ondalık adet reddi [DB integer zayıf]+servis/RBAC/RPC; +danışman done-öncesi: tip yalan footgun — `component_name` runtime taşınıyordu ama `ComponentShortage`+`CompleteProductionResult.shortages` tipleri omit ediyordu→gelecek "tip temizliği" headline'ı sessizce düşürür, testler yeşil kalırdı; fix iki tipe `component_name?`+service sözleşme testi; +16 test yeni `production-shortage-helpers.test.ts`[6]+`production-ui.test.ts`[9]+`production-service`[+1] [voice test dokunulmadı], 4410→4426; GÜVENLİ salt-okuma probe 8/8 [prod 3 kayıt, MUTASYON YOK — create kalıcı entries+movements+BOM tüketimi/delete ters hareket], **COMMIT+PUSH `0504bdc`**); önceki: **Tedarikçiler sayfası final ürün** (`/dashboard/vendors`; a11y modal/drawer+`loadError` banner+toplu-seçim is_active; +8 test, probe 7/7, COMMIT+PUSH `95ad46e`); önceki: **Satın Alma Siparişleri sayfası final ürün 2. tur** (sessiz yükleme→`loadError`+banner+"Yeniden dene", expected_date→`formatExpectedDate`; 4402, probe 7/7, COMMIT+PUSH `448c548`); önceki: **Paraşüt Sync sayfası final ürün** (kırık Manuel Sync→`/api/parasut/sync-pending`; COMMIT+PUSH `ca34198`); önceki: Teklif V7 **Faz 8 — Ertelenen Borçlar Kapanışı** (5 alt-faz/commit): 8a RBAC yazma uçları (`4935e88`), 8b convert ölü kod temizliği (`71a22cd`), 8c quotes audit katmanı helper-seviyesi (`034f8ea`), 8d order_line_description migration 080 (`4218d3e`, APPLY EDİLDİ ✅), 8e Paraşüt iskonto orantılı guard→reconciliation (`4b9c938`). Kullanıcı: sig rename ATLA, drag-reorder ERTELE. **+Bulgular 1.tur (`f7fc9f8`): reconcile guard invoice retry yoluna da uygulandı + 080 doc flip + P3 yorumlar. 4215 test, COMMIT+PUSH EDİLDİ · 080 APPLY EDİLDİ ✅**; **V7 + tüm ertelenen borçlar TAMAMLANDI**; önceki: Faz 7 note_templates (079 APPLY EDİLDİ ✅)

## Reference
- [reference_live_demo_redaction.md](reference_live_demo_redaction.md) — Canlı/demo'da null fiyatlar = RBAC redaction (viewer rolü), BUG DEĞİL; auth gate `src/proxy.ts` (middleware.ts değil)

## Roadmap
- [project_voice_input.md](project_voice_input.md) — Sesli üretim girişi V1-V3 ✅ 3657 test; V3: fireNotes→notlar entegrasyonu + Ctrl+M shortcut (input/processing/demo/repeat guard'lı); pure helper voice-note-helpers.ts (client/server boundary korunur)
- [yuksek_etki_plan.md](yuksek_etki_plan.md) — 4 yüksek etkili stok özelliği: 3/4 tamamlandı; kalan: Tedarikçi Performansı (düşük öncelik)
- [project_quotes.md](project_quotes.md) — Teklif modülü: V7 master plan; Faz 1a/1b/2/3 ✅ + Faz 5 numara (073) ✅ + Revizyon (074) ✅ + Faz 4 PDF Arşiv (075-076) ✅ + **Faz 6 Accept→Sipariş (077 atomik + 078 qty-fix; tek `/accept`; eski 2 yol→410; donmuş totaller; V7-A5 recover/A4 Paraşüt guard/A8-A11) + Bulgular 1.+2. tur** (1.tur: phantom recover/iskonto+KDV UI/accept RBAC/RPC qty 078/doc; 2.tur: arşiv create-race obje-doğrulama [tri-state]/accept fail-closed unknown→502/order arşiv PDF linki/doc/lint; 3.tur 3×P3: doc drift/archive route stale yorum/emoji kalsın) — 4043 test, `9b9ecde` 077/078 APPLY EDİLDİ ✅ + **Faz 7 Not Şablonları (note_templates, migration 079 — 080 KALICI DÜŞÜRÜLDÜ: position zaten var; CRUD GET-açık/mutation-admin/soft-delete + settings sayfası + QuoteForm 3-alan picker + PMT seed) 4098 test, 079 APPLY EDİLDİ ✅ — V7 master-plan TAMAMLANDI** + **Faz 8 Ertelenen Borçlar Kapanışı** (8a RBAC / 8b convert-cleanup / 8c audit helper-seviyesi / 8d order_line_description mig.080 / 8e Paraşüt iskonto orantılı reconciliation; sig-rename ATLA + drag-reorder ERTELE) + **Bulgular 1.tur** (reconcile guard invoice retry yoluna da / 080 doc flip / P3 yorumlar; `f7fc9f8`) 4215 test, COMMIT+PUSH EDİLDİ · 080 APPLY EDİLDİ ✅; KALAN (quotes borcu DEĞİL): audit actor (trigger), GET view_quotes RBAC, Paraşüt Sandbox GATE
- [project_frontend_renewal.md](project_frontend_renewal.md) — Frontend yenileme planı (frontend-renewal.md): DOM mutation fix, component lib, a11y — PLAN HAZIR, uygulama başlamadı

## Feedback
- [feedback_no_silent_deletes.md](feedback_no_silent_deletes.md) — Kod silmeden önce onay veya net gerekçe gerekiyor
- [feedback_memory_updates.md](feedback_memory_updates.md) — MEMORY.md ve memory dosyaları düzenli güncel tutulmalı
- [feedback_auto_context_update.md](feedback_auto_context_update.md) — current_focus.md ve CLAUDE.md Mevcut Durum her iş sonunda otomatik güncellenmeli
- [feedback_plan_domain_check.md](feedback_plan_domain_check.md) — Plan yazmadan önce domain-rules.md okunmalı; projeden kopuk özellik planlanmamalı
